## systems

1. run main(`lap_tap_key`) program as sudo because require sudo to read key inputs
1. when typed, main call `lap_tap_audio` to play tap audio as normal user

## issues

- [ ] delay sound -> async?
- [ ] resolve path problem (now, using fullpath and hardcode...)
